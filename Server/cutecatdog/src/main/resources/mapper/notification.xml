<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cutecatdog.mapper.BoardMapper">

    <select id="selectNotificationList" parameterType="int" resultType="DiaryDto">
		SELECT id, user_id, type, title, content, datetime
		FROM tbl_notification
        WHERE user_id = #{userId}
        order by datetime desc
	</select>

    <select id="selectNotificationN" parameterType="int" resultType="DiaryDto">
		SELECT id, user_id, type, title, content, datetime
		FROM tbl_notification
        WHERE user_id = #{userId}
        order by datetime desc and type = 1
	</select>

    <select id="selectNotificationE" parameterType="int" resultType="DiaryDto">
		SELECT id, user_id, type, title, content, datetime
		FROM tbl_notification
        WHERE user_id = #{userId}
        order by datetime desc and type = 2
	</select>

    <select id="selectNotificationP" parameterType="int" resultType="DiaryDto">
		SELECT id, user_id, type, title, content, datetime
		FROM tbl_notification
        WHERE user_id = #{userId}
        order by datetime desc and type = 3
	</select>

    <insert id="insertNotification" parameterType="DiaryDto" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO tbl_notification (user_id, type, title, content, datetime)
		VALUES (#{userId},#{type}, #{title},#{content},#{datetime})
	</insert>

    <!-- <update id="updateNotification" parameterType="DiaryDto">
        UPDATE tbl_notification
        SET type = #{type}, title = #{title}, content = #{content}, datetime = #{datetime}
        WHERE id = #{id}
    </update> -->

	<delete id="deleteNotification" parameterType="int">
		DELETE FROM tbl_notification WHERE id = #{id}
	</delete>

</mapper>